"""
A Company on our platform.

// Done (3) - added a resolver to make the employees field work. This query below works.

query Companies {
  companies {
    id
    name
    color
    image
    employees {
      name
      image
      id
      friends {
        name
        id
      }
    }
  }
}
"""
type Company {
  id: ID!
  color: Color
  image: String!
  name: String!
  employees: [User!]
}
